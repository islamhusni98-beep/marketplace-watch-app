import { chromium } from 'playwright';
import fs from 'node:fs/promises';import path from 'node:path';
const SEARCH_URL=process.env.HATLA2EE_URL||'https://eg.hatla2ee.com/en/car/city/giza',TELEGRAM_BOT_TOKEN=process.env.TELEGRAM_BOT_TOKEN,TELEGRAM_CHAT_ID=process.env.TELEGRAM_CHAT_ID,MAX_ITEMS=Number(process.env.MAX_ITEMS||60);if(!TELEGRAM_BOT_TOKEN||!TELEGRAM_CHAT_ID)throw new Error('Telegram config missing');
const dataDir=path.resolve('data'),seenPath=path.join(dataDir,'seen-hatla2ee.json');await fs.mkdir(dataDir,{recursive:true});let seen=new Set();try{seen=new Set(JSON.parse(await fs.readFile(seenPath,'utf8')))}catch{}const cairoToday=new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Cairo',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());
function norm(s=''){return s.toLowerCase().replace(/[\-_]/g,' ').replace(/\s+/g,' ').trim()}function targetCar(text=''){const t=norm(text),years=(t.match(/(?:19|20)\d{2}/g)||[]).map(Number),y=years.find(v=>v>=1990&&v<=2030);let name,min,max=2026,manual=false;if(/sunny|صني/.test(t)){name='Nissan Sunny';min=2016}else if(/aveo|افيو/.test(t)){name='Chevrolet Aveo';min=2016}else if(/(?:new\s*)?optra|اوبترا|أوبترا/.test(t)){name='Chevrolet New Optra';min=2016}else if(/accent\s*rb|اكسنت\s*(?:ار\s*بي|rb)/.test(t)){name='Hyundai Accent RB';min=2016}else if(/(?:\bbyd\b|بي\s*واي\s*دي).*(?:\bf3\b|اف\s*3)|(?:\bf3\b|اف\s*3).*(?:\bbyd\b|بي\s*واي\s*دي)/.test(t)){name='BYD F3';min=2021}else if(/elantra\s*hd|النترا\s*(?:اتش\s*دي|hd)/.test(t)){name='Hyundai Elantra HD';min=2017}else if(/verna|فيرنا/.test(t)){name='Hyundai Verna';min=2016}else if(/(?:dayun|دايون).*(?:lanos|لانوس)|(?:lanos|لانوس).*(?:dayun|دايون)/.test(t)){name='Dayun Lanos';min=2016}else if(/lanos|لانوس/.test(t)){name='Chevrolet Lanos';min=2016}else if(/arrizo\s*5|اريزو\s*(?:5|فايف)/.test(t)){name='Chery Arrizo 5';min=2019;manual=true}if(!name||!y||y<min||y>max)return null;if(manual&&!/manual|man\.?|مانيوال|يدوي/.test(t))return null;return{name,year:y}}
function isGiza(t=''){return /giza|sheikh zayed|zayed|6 october|october|haram|dokki|mohandessin|agouza|faisal|imbaba|pyramids gardens|hadayek october/i.test(t)}
async function sendTelegram(i){const text=['🚗 إعلان سيارة مستعملة جديد','🟣 التصنيف: TODAY','🌐 المصدر: Hatla2ee','',`🎯 ${i.target.name} ${i.target.year}`,`📌 ${i.title}`,`💰 السعر: ${i.priceText||'غير ظاهر'}`,`📅 تاريخ النشر: ${i.postedOn}`,`📍 المكان: ${i.location}`,`🔗 رابط الإعلان: ${i.url}`].join('\n');const r=await fetch(`https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({chat_id:TELEGRAM_CHAT_ID,text,disable_web_page_preview:false})});if(!r.ok)throw new Error(`Telegram ${r.status}: ${await r.text()}`)}
const browser=await chromium.launch({headless:true});const context=await browser.newContext({locale:'en-US',userAgent:'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/124 Safari/537.36'}),search=await context.newPage();await search.goto(SEARCH_URL,{waitUntil:'domcontentloaded',timeout:60000});await search.waitForTimeout(3000);for(let i=0;i<6;i++){await search.mouse.wheel(0,2200);await search.waitForTimeout(600)}const candidates=await search.locator('a[href*="/en/car/"]').evaluateAll(links=>{const out=[],s=new Set();for(const a of links){const href=a.href||'';if(!/\/en\/car\/[^/]+\/[^/]+\/\d+\/?(?:\?.*)?$/i.test(href))continue;const clean=href.split('?')[0].split('#')[0],id=clean.match(/\/(\d+)\/?$/)?.[1];if(!id||s.has(id))continue;s.add(id);out.push({id,href:clean})}return out});await search.close();const unseen=candidates.filter(c=>!seen.has(`Hatla2ee:${c.id}`)).slice(0,MAX_ITEMS);let sent=0,targeted=0;for(const c of unseen){const page=await context.newPage();try{await page.goto(c.href,{waitUntil:'domcontentloaded',timeout:60000});await page.waitForTimeout(900);const body=(await page.locator('body').innerText().catch(()=>''))||'',title=(await page.locator('h1').first().innerText().catch(()=>''))||'Hatla2ee listing',condition=body.match(/Condition\s*\n\s*([^\n]+)/i)?.[1]?.trim()||'',postedOn=body.match(/Posted On\s*\n\s*(\d{4}-\d{2}-\d{2})/i)?.[1]||'',location=body.match(/Location\s*\n\s*([^\n]+)/i)?.[1]?.trim()||'',priceText=body.match(/[0-9][0-9,]*\s*EGP/i)?.[0]||'',target=targetCar(`${title}\n${body}`);if(!target)continue;targeted++;if(!/^used$/i.test(condition)||postedOn!==cairoToday||!isGiza(location))continue;const id=`Hatla2ee:${c.id}`;await sendTelegram({id,url:c.href,title,postedOn,location,priceText,target});seen.add(id);sent++}catch(e){console.warn(`Hatla2ee ${c.id}: ${e.message}`)}finally{await page.close()}}await fs.writeFile(seenPath,JSON.stringify([...seen].slice(-5000),null,2));console.log(`Hatla2ee: targetCars=${targeted}, sent=${sent}`);await browser.close();